#include "Room.h"
#include "GameDefines.h"

using namespace std;

void Room::Description() {
	if (m_mapPosition.x == ROOM_KITCHEN.x && m_mapPosition.y == ROOM_KITCHEN.y) {

	}
	if (m_mapPosition.x == ROOM_HALLWAY_NORTH.x && m_mapPosition.y == ROOM_HALLWAY_NORTH.y) {

	}
	if (m_mapPosition.x == ROOM_STUDY.x && m_mapPosition.y == ROOM_STUDY.y) {

	}
}
